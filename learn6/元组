
元组：
    元组是不可变的序列，元组由不同的元素组成，每个元素可以存储不同类型的数据，元组通常代表一行数据，而元组中的元素则代表不同的数据项
    一旦创建后则不能修改长度

    元组如何可变？
        元组和列表的组合，元组不可变，但是元组中包含的可变对象仍然可以变，就像二维数组一样
    引用场景：
        如果我们的数据传给了一个不了解的api接口，同时不希望数据被改变，就可以使用元组