
字典：
    是Python语言中唯一的映射类型，映射就是一组key和value之间的映射函数，其特点是：key的唯一性，key与value的一对多映射
    字典的键值：
        不允许一个键值对应多个值，如果初始化或者更新过程中出现多个键值，会存储最后一个对应值，其他的会自动删除
        键值必须可以hash：
            Python解释器调用hash函数，根据字典中的键值来存储数据位置，如果键值改变哈希函数会映射到不同地址来存储数据
            这样哈希函数就不能准确的存储或者获取相关数据
    字典操作：
        访问，更新，删除
    字典标准函数：
        len     in/not in    str,list,tuple  ---  将字典key转为str，list，tuple类型
        比较运算符与cmp函数
    字典相关方法：
        字典的嵌套，val值可以用另一个字典代替，形成嵌套结构
        以及各种'.'之后的方法，详见test
哈希表：
    是根据关键码值而直接进行访问的数据结构，列表，字典不能被hash    ---     可修改的类型都是不能被hash的